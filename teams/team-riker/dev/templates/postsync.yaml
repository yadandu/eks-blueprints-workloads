apiVersion: batch/v1
kind: Job
metadata:
  name: pi
  annotations:
    argocd.argoproj.io/hook: PostSync
spec:
  template:
    spec:
      containers:
      - name: pi
        image: busybox
        command: ["/bin/sh", "-ec", "mkdir yamuna", "sleep 1000"]
      restartPolicy: Never
  backoffLimit: 1
